[
    Test 2
    Poprawne zwijanie stałych, i przy okazji dobra obsługa tablic
    
    Przykladowe dane testowe:
        1. ? 0
           > 0
        2. ? 30
           > 1
]
DECLARE
	a, b, tab(0:2)
BEGIN
    READ a;
    b := a / a;
    tab(0) := 0;
    tab(1) := 1;
    WRITE tab(b);
END
